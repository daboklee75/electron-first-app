# 컴포넌트 연구 작업 목록 (Vanilla JS)

- [x] 저장소 설정
    - [x] `main`에서 `develop` 브랜치 생성
    - [x] `develop`에서 `feature/login` 브랜치 생성
- [x] 환경 설정
    - [x] 폴더 구조 생성 (`js/components`, `css`)
    - [x] 스타일링을 위한 index.css 설정
- [x] 컴포넌트 구현 (Vanilla JS)
    - [x] `TopComponent.js` ("First App System" 헤더)
    - [x] `CenterComponent.js` (중앙 컨테이너 및 화면 전환 로직)
    - [x] `LoginReady.js` ("로그인해주세요" 화면)
    - [x] `LoginForm.js` (ID 입력 화면)
    - [x] `MainDashboard.js` (로그인 후 대시보드)
    - [x] `main.js` (앱 진입점 및 상태 초기화)
- [x] 검증
    - [x] `electron .` 실행
    - [x] 흐름 확인: 로그인 준비 -> 클릭 -> 로그인 입력 -> 확인 -> 메인 대시보드
